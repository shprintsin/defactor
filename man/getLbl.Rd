% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setLabel.R
\name{getLbl}
\alias{getLbl}
\title{Get Label for a Column}
\usage{
getLbl(df, col, suffix = "_labels", limit = 0)
}
\arguments{
\item{df}{A data frame containing the column.}

\item{col}{The column name for which labels are to be retrieved.}

\item{suffix}{A string to append to the label column name (default: "_labels").}

\item{limit}{The maximum number of labels to return (default: 0, which means all labels).}
}
\value{
A data frame with the column values and their corresponding labels.
}
\description{
Retrieves the labels of a specified column in a data frame.
}
\details{
The function retrieves the \code{labels} attribute of a factor variable and uses it to create a new column with descriptive labels. The new column name is either specified by the \code{bycol} parameter or dynamically generated using the \code{suffix} parameter.
}
\examples{
# Create a sample data frame with a factor column
df <- data.frame(
  category = factor(c(1, 2, 1, 3)),
  value = c(10, 20, 15, 25)
)

# Assign labels to the factor column
attr(df$category, "labels") <- c('A' = 1, 'B' = 2, 'C' = 3)

# Use the function with the default suffix
df_with_labels <- getLbl(df, 'category')

# Use the function with a custom suffix
df_with_custom_suffix <- getLbl(df, 'category', suffix = "_desc")

print(df_with_labels)
print(df_with_custom_suffix)
}
